// Fill out your copyright notice in the Description page of Project Settings.


#include "WidgetManager.h"
#include "Components/ScrollBox.h"
#include "Components/Button.h"

/*
void UWidgetManager::ManageScrollBar()
{
	if (WidgetClass)
	{
		if (!HUD_Widget)
		{
			HUD_Widget = CreateWidget<UUserWidget>(this, WidgetClass);
		}
	}
	else
		UE_LOG(LogTemp, Error, TEXT("No Class found in WidgetManager"));

	UWidget* ScrollBox = Cast<UScrollBox>(HUD_Widget->GetWidgetFromName("Box"));

	// Assicurati che la Scroll Box sia valida
}
*/
